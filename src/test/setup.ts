// Vitest setup: runs before every test file. Currently a placeholder for
// jsdom polyfills (e.g. AudioContext, SpeechSynthesis mocks) that
// provider-level tests will need as those land.
export {};
